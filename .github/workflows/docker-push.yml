# To trigger pipeline push a new tag

on:
  push:
    tags:
      - '*'

env:
  DOCKERHUB_USERNAME: ${{ vars.DOCKERHUB_USERNAME }}

jobs:
  docker_push:
    runs-on: ubuntu-22.04
    name: Deploy Image
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Get Tag
        run: echo "TAG=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV
      - name: Login to Docker Hub as ${{ env.DOCKERHUB_USERNAME }}
        uses: docker/login-action@v3
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build Image
        run: IMAGE=docker.io/${{ env.DOCKERHUB_USERNAME }}/istio-operator:${{ env.TAG }} make image
      - name: Push Image
        run: docker push docker.io/${{ env.DOCKERHUB_USERNAME }}/istio-operator:${{ env.TAG }}
